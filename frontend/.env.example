# Base URL the frontend uses to reach the backend API.
# Copy this file to .env.development.local for local development against a
# local backend (e.g. http://localhost:5000/api), or set REACT_APP_API_URL in
# your hosting provider's environment settings for production.
REACT_APP_API_URL=https://splitr-2g63.onrender.com/api
